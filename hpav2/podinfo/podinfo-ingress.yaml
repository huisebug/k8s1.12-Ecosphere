apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: podinfo
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: podinfo.huisebug.com
    http:
      paths:
      - path: "/"
        backend:
          serviceName: podinfo-service
          servicePort: 9898
